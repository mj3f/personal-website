image: docker:latest

services:
  - docker:dind

stages:
  - build
  - publish

build:
  stage: build
  script:
    - echo "Building image"
    - docker build --tag personal-website:latest .

publish:
  stage: publish
  script:
    - echo "Publishing built image"
    - docker push mikepratt/personal-website:latest