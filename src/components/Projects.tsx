import React, { FunctionComponent } from 'react';

const Projects: FunctionComponent = () => (
    <div className="text-center">
        <h1 className="text-3xl font-semibold p-5">Projects</h1>
        <a href="https://fpl.msjp.dev/" className="hover:text-green-500">Fantasy Premier League API wrapper written in .NET Core (unfinished).</a>
    </div>
);

export default Projects;